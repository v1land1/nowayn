import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
	container: {
		padding: 12,
		backgroundColor: '#fff',
		borderRadius: 8,
		marginTop: 12,
		elevation: 1,
	},
	line: {
		fontSize: 16,
		color: '#222',
		marginBottom: 4,
	},
});
